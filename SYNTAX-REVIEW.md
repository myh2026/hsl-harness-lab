# HSL 语法舒适度实测评估

> 评估人：独立 AI 工程师（未参与 HSL 开发） · 评估基准：为毕业论文实测撰写
> 两个完整 harness 项目（MathPhysLab 数理验证 / ArmorLab 红蓝对抗，共
> 5 + 3 个 HSL 模块、~700 行 HSL 源码）+ 12 组语法探针 · 工具链版本
> dhv-ts 0.2.50（评估期）→ 0.2.51（修复后复测）
>
> 方法论：不做纸面评审。每个论断都来自真实代码的撰写、运行、投射与
> 调试过程；所有 bug 先最小化复现再提交结论；修复后全部复测。

---

## 一、总体判断

**HSL 是一门「为 harness 而生的领域语言」，其领域投资回报真实存在；
通用语言部分是 Rust 的严格子集，舒适度取决于你对 Rust 的亲近度。**
写 harness 核心编排（graph/edge/AgentLoop/剧本测试）时的体验显著优于
用 Python 手写同等结构；写数据处理与字符串逻辑时的摩擦则主要来自
Rust 式的显式性税。评估期共触发 8 个工具链真实缺陷（详见 §4）——
其中 3 个属于「静默错误」级别（对数值 harness 是致命的），这提醒我们：
这门语言的工程成熟度还在早期，但**架构设计的前瞻性是罕见的**。

| 维度 | 评分 | 一句话 |
|:---|:---:|:---|
| harness 表达力（拓扑/协议/观测） | 5/5 | graph/edge/AgentLoop/剧本/G6 事件，没有等价物 |
| 错误信息质量 | 4.5/5 | 带行列号、带候选名、带修复建议，超出预期 |
| 多后端投射可信度（0.2.51 后） | 4/5 | 全链生成物与解释器语义逐位一致；contract 回退诚实 |
| 语法人体工学 | 3.5/5 | Rust 用户宾至如归；`String::from`/`.clone()` 是持续摩擦 |
| 静态安全网 | 3.5/5 | S 规则铁律有效；E-2 修复前有大洞，修复后合格 |
| 工具链稳定性 | 4/5（修后） | 评估期 3/5：8 个真实 bug，但全部可修且修后零回归 |

---

## 二、舒适的部分（附实例）

### 2.1 AgentLoop + S-6 穷尽性：架构压力是真的

写 ArmorLab 主循环时，`Move` 协议只有 3 个变体，match 漏掉 `Done`
会在 check 阶段被 S-6 直接处决。更有说服力的是一次**真实的架构改形**：
最初我想在 AgentLoop 里直接 match 8 变体的 `Verdict`（禁 `_` 兜底的
S-6 铁律迫使全部列臂），写着写着发现应该把裁决逻辑收敛到普通函数
（`verifier.hsl` 的 `verdict_record`，普通函数允许兜底）—— 主循环只
分发 3 种 Move。**铁律把我推向了更干净的「协议在 fn、分发在 loop」
分层**。这是语言设计影响架构质量的直接证据，代价是初学者会经历一次
"为什么不让写 `_`" 的困惑期。

### 2.2 剧本模式的确定性 CI 体验

`--fixture` + 可复现 PRNG + 退出码约定，让两个项目各 ~50 行的
`run-tests.sh` 就完成了「注入错误 → 断言捕获 → 断言费率 → 断言评级」
的完整回归。对毕业论文场景（需要可重复实验），这是核心生产力。
`events.jsonl` 的 G6 边事件让拓扑行为可断言（「公式边事件应恰好 4 次」），
我把它直接写进了 CI。

### 2.3 rules{} 一段投射一个工程

```hsl
rules {
    struct -> "gen/py/{name}.py"  : python,
    fn     -> "gen/py/{name}.py"  : python,
    trait  -> "gen/ts/{name}.ts"  : typescript,
    block  -> "config/{name}.yml" : yaml,
}
```
14 行 project 声明投射出 44 个文件（mathphys）—— 显式映射 + 规则批量 +
R1 遮蔽优先级的组合手感很好。X-1/X-2 诚实告警在配置错误时立刻指出
「哪个类型没投射到哪个语言」，不用等运行期。

### 2.4 错误信息是这门语言的隐形资产

实测样本：`error[S-6]: match 不穷尽：Action 缺少变体 Stop`（带变体名）、
`error[E-2]: std 模块 "std/text" 无导出名 "levenshtei"（候选：split_once,
rsplit_once, …）`（带候选）、`✗ 运行期错误：match 不穷尽：值 Ok(21)
没有匹配的分支`（我误把 `parse::<i64>()` 当 Option 匹配，报错值本身
就告诉我答案）。诊断密度显著高于我日常使用的多数脚本语言工具链。

### 2.5 native 逃生舱边界清晰

`native typescript { await $host.fixture.nextAct(); }` 的按名捕获 +
`$host` 命名空间 + 路径监狱/白名单，让 IO 与协议解析的胶水层写得很快；
「native 只做效应、逻辑留在 HSL」的纪律在两个项目里都自然成立。

---

## 三、不舒适的部分（附实例）

### 3.1 `String::from` 税（最大单项摩擦）

每个字符串字面量进入 String 语境都要 `String::from("...")`（14 字符换
7 字符内容）。解释器其实宽容（直接传 `"HSL"` 能跑），但已知限制 #52
（rust 后端对字面量入 String 容器编译失败）逼你全程显式。**「开发时
绿灯、投射后红灯」的不对称**是舒适度的隐性杀手——建议 dhv 在 check
阶段对字面量进 String 语境给 warning。armorlab 的规则表 18 条签名
× `String::from`，仅此一处就多写 ~200 字符。

### 3.2 `.clone()` 遍地

无 borrow checker 的动态解释器 + Rust 语义的值传递 → `p.clone()`/
`problem.given.clone()` 成为传参默认动作。安全但啰嗦；更深的问题是
**它教不出所有权直觉**（clone 一律合法），却收了所有权语法的税。

### 3.3 API 风格不统一的小刺

- `Vec::sort()` 原地返回 unit（不能链式），`sort_desc`（std）返回新 Vec
  —— 同一语义域两种风格；
- `HashMap::get` 返回 Option → `m.get(k).unwrap_or(String::from(""))`
  的长链在协议解析里反复出现；
- format! 的 `{:.3}` 评估期被静默丢弃（0.2.51 已修），报告里
  `29.698484809834998` 这种全精度数字曾直接影响可读性。

### 3.4 写法影响投射质量（用户可感知但不可控）

同样的逻辑，尾表达式 match（`judge_formula_v`）活体翻译，多早退
return（`judge_claim_v`）回退 contract。**"函数写法决定生成物质量"**
对语言初心（逻辑层与物理层解耦）是一处渗漏——用户改写 HSL 风格来
迁就翻译器，而不是翻译器迁就用户。建议在指南里加一节「活体翻译
友好写法」。

### 3.5 嵌套枚举 match 的认知负担

AgentLoop 内嵌套枚举（如 Verdict 8 变体）需要全臂展开或外移函数。
设计意图我认可（§2.1），但**报错信息可以更助人**：目前只说"不允许
通配兜底"，可以追加建议「将嵌套枚举裁决收敛到普通函数」。

---

## 四、实测触发的工具链缺陷（8 个，全部已最小化复现）

评估的价值的一半在这里——以下全部是撰写真实 harness 时自然触发，
非构造性 fuzzing：

| # | 缺陷 | 严重度 | 触发场景 | 状态 |
|:---:|:---|:---|:---|:---:|
| 1 | 未知函数调用 check 静默通过 | **盲区级** | 漏 import `sort_desc`，check 全绿 run 才炸 | 0.2.51 E-2 已修 |
| 2 | `1.0/7.0` 整数值浮点除法静默 = 0 | **静默错误** | armorlab 规避率/误报率全为 0.000 | 0.2.51 已修 |
| 3 | `format!` `{:.3}` 精度说明符静默丢弃（interp + codegen 双端） | **静默错误** | 数值报告全精度裸奔 | 0.2.51 已修 |
| 4 | native python 块缩进敏感（嵌套 >1 层必炸；恰好对齐时末表达式被执行 N 次） | 正确性 | 多行 python 块缩进 8 空格 | 0.2.51 已修 |
| 5 | native python 末表达式因字面量内 `=` 误判为语句，返回值静默丢失 | 静默错误 | `"sqrt2=%.6f" % math.sqrt(2)` | 0.2.51 已修 |
| 6 | 宏实参重建丢字符串引号（含 `'` 直接 LexError；不含则静默变标识符） | 双重隐患 | SQL 签名 `"' or 1=1"` 进 format! 实参 | 0.2.51 已修 |
| 7 | 跨文件函数/常量/枚举变体依赖未接线（生成物 NameError 族） | **核心承诺打击** | 生成 python 跑 `inspect_payload` | 0.2.51 已修 |
| 8 | std/math 自由函数与常量在活体翻译中裸名直出 | 编译级 | 生成 python 用 `sin`/`PI` | 0.2.51 已修 |
| + | IDE：`hsl.compile` 缺必填 `--out` 必失败；高亮 6 处缺陷（38 后端仅 7 种高亮等） | 可用性 | VSCode 命令/高亮实测 | IDE 0.1.1 已修 |

> 其中 #7 直接打击「投射出真实工程仓库」的核心承诺——修复后实测：
> mathphys/armorlab 的生成 Python **全链语义与解释器逐位一致**
> （`recompute(projectile)=40.8163`、五类载荷裁决逐项相同）。
> 修复批次 110 用例回归全绿，零破坏。

---

## 五、给语言设计的建议（按优先级）

1. **字面量进 String 语境的 check 期 warning**（消解 §3.1 的绿灯/红灯
   不对称；比实现隐式转换便宜且诚实）；
2. **std 函数调用的全语言活体映射**（当前 python/ts 已修，rust/go/cpp
   触发 contract 回退——中期补方法形态改写）；
3. **`Vec::sort` 家族风格统一**（`sorted()` 返回新 Vec + `sort()` 原地，
   对齐 Python/Rust 当代惯例）；
4. **活体翻译友好写法指南**（尾表达式 match vs 早退 return 的投射
   差异写入指南，把「写法影响质量」从隐性知识变显性契约）；
5. **S-6 AgentLoop 铁律的报错增强**（附「外移到普通函数」建议）；
6. **枚举变体/函数名与目标语言生态的避撞提示**（如 `inspect` 遮蔽
   Python stdlib 导致 dataclasses 循环导入——check 期提示胜过运行期玄学）。

---

## 六、结论

**作为 harness DSL：值得投入。** 两个论文方向（数理验证 / 红蓝对抗）
都在 ~400 行 HSL 内完成了协议、拓扑、计分、报告、CI 的完整闭环，
且多后端投射经修复后真实可信——这正是「一份源码描述 harness 逻辑」
承诺的兑现。

**作为通用语言：请管理预期。** 它是 Rust 的严格子集 + harness 领域
扩展，不是替代品；显式性税（String::from/clone）是设计选择而非缺陷，
但 0.2.50 的三个静默错误级 bug 说明**动态解释器路线的代价在数值与
语义边界处**——0.2.51 修复后这个顾虑大幅缓解，dhv Rust 编译器的完整
类型推导仍是闭环的关键拼图。

一句话：**领域语法 5 分，通用体验 3.5 分，工程成熟度正在快速爬坡——
毕业论文时间窗内，它是「有故事可讲、有实验可做、有 bug 可修」的
稀有选题。**
